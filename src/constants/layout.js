/** Scroll threshold (px) before top bar hides */
export const TOP_BAR_HIDE_THRESHOLD = 80

/** Main content padding-top (matches fixed header heights) */
export const MAIN_PADDING = {
  withTopBar: 'pt-[120px] lg:pt-[132px]',
  navOnly: 'pt-14 lg:pt-16',
}

/** Mega menu overlay offset from top */
export const MEGA_MENU_TOP = {
  withTopBar: 'top-[120px] lg:top-[132px]',
  navOnly: 'top-14 lg:top-16',
}
